<h1>Listing users</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Pxxwrd</th>
    <th>Email address</th>
    <th>Code helps count</th>
    <th>Information</th>
    <th>Fav lang</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.pxxwrd %></td>
    <td><%= user.email_address %></td>
    <td><%= user.code_helps_count %></td>
    <td><%= user.information %></td>
    <td><%= user.fav_lang %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
